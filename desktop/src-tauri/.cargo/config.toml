# 本项目使用的 crates.io 镜像（sparse 协议）。
# 直连 index.crates.io 在本机 TLS 握手失败，改用 rsproxy.cn 镜像；
# 该配置仅对本项目生效，不影响全局 cargo 设置。
[source.crates-io]
replace-with = "rsproxy-sparse"

[source.rsproxy-sparse]
registry = "sparse+https://rsproxy.cn/index/"

[net]
retry = 5
